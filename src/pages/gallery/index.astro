---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
---

<BaseLayout title="ギャラリー（大分類） - Platycerium Collection">
    <!-- Custom CSS -->
    <style is:global>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            margin: 0 !important;
            padding: 0 !important;
        }

        /* すべての余白を完全に削除 */
        header {
            margin: 0 !important;
            display: block !important;
        }

        header > div {
            padding-bottom: 0 !important;
        }

        section.mosaic-hero {
            margin: 0 !important;
            padding: 0 !important;
            display: block !important;
            position: relative;
            top: -1px;
        }

        main {
            margin-top: 0 !important;
            padding-top: 3rem;
        }

        * {
            box-sizing: border-box;
        }

        .skeleton {
            background: linear-gradient(90deg, #f0f0f0 25%, #e0e0e0 50%, #f0f0f0 75%);
            background-size: 200% 100%;
            animation: loading 1.5s infinite;
        }

        @keyframes loading {
            0% { background-position: 200% 0; }
            100% { background-position: -200% 0; }
        }

        .fade-in {
            animation: fadeIn 0.5s ease-in;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .fade-in-up {
            animation: fadeInUp 0.8s ease-out forwards;
        }

        .species-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            cursor: pointer;
            display: flex;
            flex-direction: column;
            height: 420px;
        }

        .species-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
        }

        .species-thumbnail {
            width: 100%;
            height: 240px;
            object-fit: cover;
            object-position: center center;
            display: block;
        }

        .species-card-content {
            height: 120px;
            display: flex;
            flex-direction: column;
            justify-content: flex-start;
            flex: 1;
        }

        .species-card-footer {
            height: 60px;
            display: flex;
            align-items: center;
        }

        /* 原種カード専用（コンパクト） */
        .pure-species-card {
            height: 360px;
        }

        .pure-species-card .species-thumbnail {
            height: 240px;
        }

        .pure-species-card .species-card-content {
            height: 80px;
        }

        .pure-species-card .species-card-footer {
            height: 40px;
        }

        .badge-pure {
            background-color: #e8f5e9;
            color: #2e7d32;
        }

        .badge-hybrid {
            background-color: #fff3e0;
            color: #ef6c00;
        }

        /* チップの白基調デザイン */
        .filter-chip {
            transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
            position: relative;
            background: linear-gradient(135deg, rgba(255, 255, 255, 0.95), rgba(249, 250, 251, 0.98));
            backdrop-filter: blur(5px);
            -webkit-backdrop-filter: blur(5px);
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05),
                        inset 0 1px 0 rgba(255, 255, 255, 1);
        }

        .filter-chip:hover {
            background: linear-gradient(135deg, rgba(16, 185, 129, 0.08), rgba(5, 150, 105, 0.12));
            transform: translateY(-2px);
            box-shadow: 0 4px 16px rgba(16, 185, 129, 0.15),
                        inset 0 1px 0 rgba(255, 255, 255, 1);
        }

        .filter-chip:active {
            transform: scale(0.95);
        }

        /* 横スクロールをスムーズに */
        #species-chips {
            scroll-behavior: smooth;
            -webkit-overflow-scrolling: touch;
        }

        /* ドロップダウンのモバイル対応 */
        @media (max-width: 640px) {
            #date-dropdown {
                left: 50%;
                transform: translateX(-50%);
                width: calc(100vw - 2rem);
                max-width: 300px;
            }
        }

        .accordion-header {
            cursor: pointer;
            transition: background-color 0.2s ease;
        }

        .accordion-header:hover {
            background-color: #f3f4f6;
        }

        .accordion-icon {
            transition: transform 0.3s ease;
        }

        .accordion-icon.rotate {
            transform: rotate(180deg);
        }

        .accordion-content {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.3s ease;
        }

        .accordion-content.open {
            max-height: 4000px;
        }

        .filter-item {
            transition: all 0.2s ease;
        }

        .filter-item.disabled {
            opacity: 0.4;
            cursor: not-allowed;
            pointer-events: none;
        }

        .subspecies-item {
            padding-left: 1rem;
            border-left: 2px solid #e5e7eb;
            font-size: 0.875rem;
        }

        .month-item {
            padding-left: 1rem;
            font-size: 0.875rem;
        }

        /* モザイクヒーロー */
        .mosaic-hero {
            position: relative;
            width: 100%;
            min-height: 50vh;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #f0fdf4 0%, #d1fae5 50%, #a7f3d0 100%);
        }

        /* 明るいオーバーレイ */
        .mosaic-hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(ellipse at center, rgba(255,255,255,0.8) 0%, rgba(255,255,255,0.3) 100%);
            z-index: 2;
            pointer-events: none;
        }

        /* 斜めのアクセント要素 */
        .mosaic-hero::after {
            content: '';
            position: absolute;
            top: -50%;
            right: -10%;
            width: 80%;
            height: 200%;
            background: linear-gradient(135deg,
                rgba(16, 185, 129, 0.08) 0%,
                rgba(52, 211, 153, 0.12) 50%,
                transparent 100%);
            transform: rotate(-15deg);
            z-index: 3;
            pointer-events: none;
        }

        .mosaic-grid {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            display: grid;
            grid-template-columns: repeat(18, 1fr);
            gap: 0;
            margin: 0;
            padding: 0;
            line-height: 0;
            opacity: 0.15;
            filter: saturate(0.8);
            z-index: 1;
        }

        .mosaic-tile {
            position: relative;
            width: 100%;
            aspect-ratio: 1 / 1;
            overflow: hidden;
            margin: 0;
            padding: 0;
            border: none;
            line-height: 0;
        }

        @supports not (aspect-ratio: 1 / 1) {
            .mosaic-tile {
                padding-bottom: 100%;
            }
        }

        .mosaic-tile img {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            display: block;
            margin: 0;
            padding: 0;
            border: none;
        }

        .hero-title-overlay {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            z-index: 20;
            text-align: center;
            padding: 2rem;
            width: 100%;
            max-width: 1400px;
        }

        .hero-title {
            font-size: 7rem;
            font-weight: 900;
            line-height: 0.95;
            margin-bottom: 2rem;
            letter-spacing: -0.04em;
            position: relative;
            opacity: 0;
            animation: fadeInUp 0.8s ease-out forwards;

            /* 鮮やかなグラデーション */
            background: linear-gradient(135deg,
                #059669 0%,
                #10b981 25%,
                #34d399 50%,
                #10b981 75%,
                #059669 100%
            );
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;

            /* クリーンなシャドウ */
            filter: drop-shadow(0 4px 12px rgba(16, 185, 129, 0.25))
                    drop-shadow(0 2px 6px rgba(0, 0, 0, 0.1));
        }

        /* 左右の装飾的なアクセントライン */
        .hero-title::before,
        .hero-title::after {
            content: '';
            position: absolute;
            top: 50%;
            width: 100px;
            height: 4px;
            background: linear-gradient(90deg,
                #10b981 0%,
                #34d399 50%,
                transparent 100%
            );
            box-shadow: 0 2px 8px rgba(16, 185, 129, 0.3);
        }

        .hero-title::before {
            left: -120px;
            transform: translateY(-50%);
        }

        .hero-title::after {
            right: -120px;
            transform: translateY(-50%) scaleX(-1);
        }

        .hero-subtitle {
            font-size: 1.5rem;
            font-weight: 700;
            letter-spacing: 0.15em;
            text-transform: uppercase;
            opacity: 0;
            animation: fadeInUp 0.8s ease-out 0.2s forwards;
            position: relative;
            display: inline-block;
            padding: 1rem 2.5rem;

            /* 白いクリーンなコンテナ */
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border: 2px solid rgba(16, 185, 129, 0.3);
            border-radius: 50px;

            /* テキストカラー */
            background-image: linear-gradient(135deg,
                #059669 0%,
                #10b981 50%,
                #059669 100%
            );
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;

            /* クリーンなシャドウ */
            box-shadow: 0 8px 24px rgba(16, 185, 129, 0.15),
                        inset 0 1px 0 rgba(255, 255, 255, 0.8),
                        0 0 0 1px rgba(16, 185, 129, 0.1);
        }

        @media (max-width: 1200px) {
            .mosaic-grid {
                grid-template-columns: repeat(15, 1fr);
            }
        }

        @media (max-width: 768px) {
            .mosaic-grid {
                grid-template-columns: repeat(10, 1fr);
            }

            .hero-title {
                font-size: 4rem;
                letter-spacing: -0.03em;
                -webkit-text-stroke: 2px #10b981;
                text-stroke: 2px #10b981;
            }

            .hero-title::before,
            .hero-title::after {
                width: 60px;
                height: 3px;
            }

            .hero-title::before {
                left: -80px;
            }

            .hero-title::after {
                right: -80px;
            }

            .hero-subtitle {
                font-size: 1rem;
                letter-spacing: 0.1em;
                padding: 0.75rem 1.5rem;
            }

            .mosaic-hero {
                min-height: 40vh;
            }
        }

        @media (max-width: 640px) {
            .hero-title {
                font-size: 3rem;
                -webkit-text-stroke: 1.5px #10b981;
                text-stroke: 1.5px #10b981;
            }

            .hero-title::before,
            .hero-title::after {
                display: none;
            }

            .hero-subtitle {
                font-size: 0.75rem;
                letter-spacing: 0.08em;
                padding: 0.6rem 1.2rem;
            }
        }

        @media (max-width: 480px) {
            .mosaic-grid {
                grid-template-columns: repeat(8, 1fr);
            }

            .hero-title {
                font-size: 2.5rem;
            }

            .hero-subtitle {
                font-size: 0.65rem;
                padding: 0.5rem 1rem;
            }
        }

        @media (max-width: 1024px) {
            aside {
                position: static !important;
            }
        }
    </style>

<Header />

<!-- Mosaic Hero Section -->
<section class="mosaic-hero">
    <div id="mosaic-gallery" class="mosaic-grid">
        <!-- モザイクタイルがここに動的に生成されます -->
    </div>
    <div class="hero-title-overlay">
        <h1 class="hero-title">ビカクシダ栽培記録</h1>
        <p class="hero-subtitle" id="gallery-stats">Platycerium Growing Records</p>
    </div>
</section>

<!-- Page Description -->
<section class="relative py-16 md:py-20 overflow-hidden" style="background: linear-gradient(135deg, #ffffff 0%, #f0fdf4 50%, #ecfdf5 100%);">
    <!-- 背景装飾（ライトグロー） -->
    <div class="absolute inset-0 opacity-30">
        <div class="absolute top-10 left-10 w-64 h-64 bg-emerald-100 rounded-full blur-3xl"></div>
        <div class="absolute bottom-10 right-10 w-80 h-80 bg-emerald-50 rounded-full blur-3xl"></div>
    </div>

    <div class="container mx-auto px-4 relative z-10">
        <div class="max-w-6xl mx-auto">
            <!-- 白いクリーンカード -->
            <div class="relative backdrop-blur-sm bg-white/80 rounded-3xl p-8 md:p-12 border-2 border-emerald-500 shadow-xl"
                 style="box-shadow: 0 8px 32px rgba(16, 185, 129, 0.08), inset 0 1px 0 rgba(255, 255, 255, 0.9);">

                <div class="flex items-start gap-8 md:gap-12">
                    <!-- ビカキンちゃん -->
                    <div class="hidden md:block flex-shrink-0 w-48 md:w-56 lg:w-64 relative group">
                        <img src="/images/bikakin-happy-01.png" alt="ビカキンちゃん"
                             class="w-full transition-all duration-300 group-hover:scale-110 group-hover:rotate-2"
                             style="filter: drop-shadow(0 10px 30px rgba(16, 185, 129, 0.3));">
                        <!-- クリーン吹き出し -->
                        <div class="absolute left-full ml-6 top-1/4 opacity-0 group-hover:opacity-100 transition-all duration-300 z-20 pointer-events-none transform group-hover:-translate-x-2">
                            <div class="backdrop-blur-sm bg-white/95 border-2 border-emerald-500 rounded-2xl px-5 py-3 whitespace-nowrap text-sm font-bold text-emerald-700"
                                 style="box-shadow: 0 4px 20px rgba(16, 185, 129, 0.15), inset 0 1px 0 rgba(255, 255, 255, 0.8);">
                                成長が楽しいビカ！
                            </div>
                        </div>
                    </div>

                    <!-- 説明文 -->
                    <div class="flex-1">
                        <p class="text-lg md:text-xl text-gray-700 leading-relaxed mb-4 font-medium">
                            Instagramに投稿した<span class="text-emerald-600 font-bold">栽培記録</span>をまとめました。自分の備忘録として作ったんですが、ビカクシダに興味がある人に役立つかなと思って公開してます。
                        </p>
                        <p class="text-base md:text-lg text-gray-600 leading-relaxed mb-4">
                            このサイトの特徴は、<span class="text-emerald-600 font-semibold">購入した時点から現在の姿まで</span>時系列で見られることです。フィルターで原種や投稿時期を絞り込めば、「<span class="text-emerald-600 font-semibold">だいたいこれくらいでこんな感じになるんだな</span>」っていうイメージがつかめると思います。
                        </p>
                        <p class="text-base md:text-lg text-gray-600 leading-relaxed mb-4">
                            ご自身のビカクシダの成長速度と見合わせて<span class="text-emerald-600 font-semibold">ベンチマーク</span>にしたり、「今後うちの株もこうなるのかな」って想像するのに使ってみてください。
                        </p>
                        <p class="text-base md:text-lg text-gray-600 leading-relaxed mb-4">
                            ビカクシダって<span class="text-emerald-600 font-semibold">環境や育て方で姿が全然変わる</span>んです。その多様性とゲーム性が面白い。難しい品種はあるにはありますけど、それも含めて楽しいですよ。
                        </p>
                        <div class="flex items-start gap-3 text-sm text-gray-500 mt-6 pt-4 border-t border-emerald-400">
                            <img src="/images/bikakin-sad-01.png" alt="悲しむビカキンちゃん"
                                 class="w-16 h-16 flex-shrink-0"
                                 style="filter: drop-shadow(0 4px 12px rgba(16, 185, 129, 0.2));">
                            <p class="mt-1">※ 途中で記録が止まっているものは、正直に言うと、すでに手元にないです（枯らした・売った・あげた等）。</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<main class="container mx-auto px-4 py-8" style="background: linear-gradient(180deg, #ffffff 0%, #f9fafb 50%, #f3f4f6 100%); min-height: 100vh;">
    <!-- Species Category Chips -->
    <div class="mb-8">
        <div class="overflow-x-auto pb-2 -mx-4 px-4">
            <div id="species-chips" class="flex gap-2 min-w-max">
                <!-- チップが動的に生成される -->
            </div>
        </div>
    </div>

    <!-- Main Content -->
    <div class="max-w-7xl mx-auto">
                <!-- Loading Indicator -->
                <div id="loading" class="text-center py-12">
                    <div class="inline-flex items-center gap-2 text-gray-600">
                        <svg class="animate-spin h-8 w-8" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                            <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                            <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                        </svg>
                        <span class="text-lg">品種データを読み込んでいます...</span>
                    </div>
                </div>

                <!-- Error Message -->
                <div id="error" class="hidden text-center py-12">
                    <p class="text-xl text-red-600 mb-4"></p>
                    <button onclick="location.reload()" class="bg-forest-mid hover:bg-forest-dark text-white font-semibold py-2 px-6 rounded-lg transition-colors">
                        再読み込み
                    </button>
                </div>

                <!-- Pure Species Section -->
                <div id="pure-species-section" class="hidden mb-12">
                    <div class="mb-8">
                        <div class="flex items-center gap-3 backdrop-blur-sm bg-white/90 border-2 border-emerald-600 rounded-2xl px-8 py-4 mb-3 w-fit"
                             style="box-shadow: 0 4px 20px rgba(16, 185, 129, 0.12), inset 0 1px 0 rgba(255, 255, 255, 1);">
                            <img src="/images/bikakin-observing-01.png" alt="観察中ビカキンちゃん" class="w-10 h-10"
                                 style="filter: drop-shadow(0 4px 12px rgba(16, 185, 129, 0.3));">
                            <h3 class="text-3xl font-black text-emerald-700 tracking-tight">
                                原種
                            </h3>
                        </div>
                        <p class="text-gray-600 text-sm ml-2">
                            自然界に存在するビカクシダの原種とその選抜品種
                        </p>
                    </div>
                    <div id="pure-species-grid" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
                        <!-- Pure species cards will be rendered here -->
                    </div>
                </div>

                <!-- Hybrid Species Section -->
                <div id="hybrid-species-section" class="hidden">
                    <div class="mb-8">
                        <div class="flex items-center gap-3 backdrop-blur-sm bg-white/90 border-2 border-orange-600 rounded-2xl px-8 py-4 mb-3 w-fit"
                             style="box-shadow: 0 4px 20px rgba(249, 115, 22, 0.12), inset 0 1px 0 rgba(255, 255, 255, 1);">
                            <img src="/images/bikakin-excited-01.png" alt="興奮中ビカキンちゃん" class="w-10 h-10"
                                 style="filter: drop-shadow(0 4px 12px rgba(249, 115, 22, 0.3));">
                            <h3 class="text-3xl font-black text-orange-700 tracking-tight">
                                交配種
                            </h3>
                        </div>
                        <p class="text-gray-600 text-sm ml-2">
                            原種同士の交配によって生まれた品種
                        </p>
                    </div>
                    <div id="hybrid-species-grid" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
                        <!-- Hybrid group card will be rendered here -->
                    </div>
                </div>
    </div>
</main>

<Footer />

    <!-- JavaScript -->
    <script is:inline type="module">
        // 原種名マッピング（英語表記）
        const speciesNameMap = {
            'bifurcatum': 'P. bifurcatum',
            'willinckii': 'P. willinckii',
            'coronarium': 'P. coronarium',
            'ridleyi': 'P. ridleyi',
            'wandae': 'P. wandae',
            'superbum': 'P. superbum',
            'veitchii': 'P. veitchii',
            'hillii': 'P. hillii',
            'alcicorne': 'P. alcicorne',
            'elephantotis': 'P. elephantotis',
            'ellisii': 'P. ellisii',
            'holttumii': 'P. holttumii',
            'stemaria': 'P. stemaria',
            'andinum': 'P. andinum',
            'quadridichotomum': 'P. quadridichotomum',
            'grande': 'P. grande',
            'wallichii': 'P. wallichii',
            'madagascariense': 'P. madagascariense',
            'hybrid': '交配種 (Hybrids)'
        };

        // グローバル状態
        let allSpeciesData = [];
        let currentCategory = 'all'; // 選択中のカテゴリ ('all', 原種ID, 'hybrid')

        // モザイクギャラリーを生成
        function renderMosaicGallery(allSpecies) {
            const mosaicContainer = document.getElementById('mosaic-gallery');
            if (!mosaicContainer) return;

            // 全品種を1つの配列に統合
            let allTiles = [];

            allSpecies.forEach(item => {
                // 原種の場合は園芸品種を展開
                if (item.type === 'pure' && item.subSpecies && item.subSpecies.length > 0) {
                    item.subSpecies.forEach(sub => {
                        allTiles.push({
                            id: sub.id,
                            name: sub.displayName || sub.name,
                            image: sub.latestImage,
                            type: 'subspecies'
                        });
                    });
                } else {
                    // 交配種または園芸品種なし原種
                    allTiles.push({
                        id: item.id,
                        name: item.displayName || item.name || speciesNameMap[item.id],
                        image: item.latestImage,
                        type: item.type
                    });
                }
            });

            // 最後の4枚を削除
            allTiles = allTiles.slice(0, -4);

            // モザイクタイルを生成
            const tilesHTML = allTiles.map(tile => {
                const imageSrc = tile.image ? `/${tile.image}` : '/images/placeholder-leaf.svg';

                return `
                    <div class="mosaic-tile">
                        <img src="${imageSrc}"
                             alt="${tile.name}"
                             loading="lazy"
                             onerror="this.src='/images/placeholder-leaf.svg'">
                    </div>
                `;
            }).join('');

            mosaicContainer.innerHTML = tilesHTML;
        }

        // 階層構造インデックス読み込み
        async function loadMainSpecies() {
            const loading = document.getElementById('loading');
            const error = document.getElementById('error');
            const stats = document.getElementById('gallery-stats');
            const pureSection = document.getElementById('pure-species-section');
            const hybridSection = document.getElementById('hybrid-species-section');
            const pureGrid = document.getElementById('pure-species-grid');
            const hybridGrid = document.getElementById('hybrid-species-grid');

            try {
                const response = await fetch('/data/species-hierarchy-index.json');
                if (!response.ok) {
                    throw new Error(`HTTP ${response.status}`);
                }

                const data = await response.json();
                console.log('Loaded data:', data);

                // hierarchy配列から原種と交配種を分離
                const hierarchyData = data.hierarchy || [];
                allSpeciesData = hierarchyData;

                // モザイクギャラリーを生成
                renderMosaicGallery(hierarchyData);

                // カテゴリチップを構築
                buildCategoryChips(hierarchyData);

                // 初期表示
                displayCategory('all');

                loading.classList.add('hidden');

                // URLハッシュがある場合、該当セクションまでスクロール
                checkUrlHashAndScroll();

            } catch (err) {
                console.error('データ読み込みエラー:', err);
                loading.classList.add('hidden');
                error.classList.remove('hidden');
                error.querySelector('p').textContent = `データの読み込みに失敗しました: ${err.message}`;
            }
        }

        // カテゴリチップを構築
        function buildCategoryChips(hierarchyData) {
            const container = document.getElementById('species-chips');
            if (!container) return;

            // 投稿がある原種のみ（純粋な原種のみ、交配種は除外）
            const pureSpecies = hierarchyData.filter(item =>
                item.type === 'pure' && item.totalPosts > 0
            );

            // 交配種があるかチェック
            const hasHybrids = hierarchyData.some(item => item.type === 'hybrid');

            // チップHTML生成
            let chips = [];

            // 「すべて」チップ
            chips.push(`
                <button class="filter-chip px-4 py-2 border-2 border-emerald-500 rounded-full text-sm font-semibold text-gray-700 transition-all whitespace-nowrap filter-active"
                        data-category="all"
                        onclick="selectCategory('all', event)">
                    すべて
                </button>
            `);

            // 各原種のチップ
            pureSpecies.forEach(species => {
                const speciesKey = species.id;
                const speciesName = speciesNameMap[speciesKey] || `P. ${speciesKey}`;

                chips.push(`
                    <button class="filter-chip px-4 py-2 border-2 border-emerald-500 rounded-full text-sm font-semibold text-gray-700 transition-all whitespace-nowrap"
                            data-category="${speciesKey}"
                            onclick="selectCategory('${speciesKey}', event)">
                        ${speciesName}
                    </button>
                `);
            });

            // 「交配種」チップ
            if (hasHybrids) {
                chips.push(`
                    <button class="filter-chip px-4 py-2 border-2 border-emerald-500 rounded-full text-sm font-semibold text-gray-700 transition-all whitespace-nowrap"
                            data-category="hybrid"
                            onclick="selectCategory('hybrid', event)">
                        交配種
                    </button>
                `);
            }

            container.innerHTML = chips.join('');
        }

        // カテゴリ選択
        window.selectCategory = function(category, event) {
            event.stopPropagation();

            // チップのアクティブ状態を更新
            document.querySelectorAll('[data-category]').forEach(btn => {
                btn.classList.remove('filter-active');
                btn.style.background = '';
                btn.style.borderColor = '';
                btn.style.boxShadow = '';
            });

            // 選択されたチップをアクティブに
            event.target.classList.add('filter-active');
            event.target.style.background = 'linear-gradient(135deg, rgba(16, 185, 129, 0.15), rgba(5, 150, 105, 0.2))';
            event.target.style.borderColor = 'rgba(16, 185, 129, 0.5)';
            event.target.style.boxShadow = '0 4px 16px rgba(16, 185, 129, 0.2), inset 0 1px 0 rgba(255, 255, 255, 1)';

            // カテゴリを表示
            currentCategory = category;
            displayCategory(category);
        }

        // 関連度でソート（最終投稿日が新しい＋投稿数が多い順）
        function sortByRelevance(a, b) {
            // まず最終投稿日で比較（新しい方が優先）
            const dateA = new Date(a.latestPostDate || a.latestImage || '2000-01-01');
            const dateB = new Date(b.latestPostDate || b.latestImage || '2000-01-01');

            if (dateA > dateB) return -1;
            if (dateA < dateB) return 1;

            // 日付が同じ場合は投稿数で比較（多い方が優先）
            const countA = a.count || a.totalPosts || 0;
            const countB = b.count || b.totalPosts || 0;
            return countB - countA;
        }

        // カテゴリ表示
        function displayCategory(category) {
            if (category === 'all') {
                // すべて：原種カードと交配種グループカードを表示
                renderAllCategories();
            } else if (category === 'hybrid') {
                // 交配種：全交配種の園芸品種カードを表示
                renderHybridSubspecies();
            } else {
                // 特定の原種：その原種の園芸品種カードを表示
                renderSpeciesSubspecies(category);
            }

            // 統計情報を更新
            updateStats(category);
        }

        // すべてのカテゴリを表示（原種カード + 交配種グループカード）
        function renderAllCategories() {
            const pureSection = document.getElementById('pure-species-section');
            const hybridSection = document.getElementById('hybrid-species-section');
            const pureGrid = document.getElementById('pure-species-grid');
            const hybridGrid = document.getElementById('hybrid-species-grid');

            const pureSpecies = allSpeciesData.filter(item => item.type === 'pure');
            const hybridSpecies = allSpeciesData.filter(item => item.type === 'hybrid');

            // 原種カード表示
            if (pureSpecies.length > 0) {
                pureGrid.innerHTML = pureSpecies.map(createSpeciesCard).join('');
                pureSection.classList.remove('hidden');
            } else {
                pureSection.classList.add('hidden');
            }

            // 交配種グループカード表示
            if (hybridSpecies.length > 0) {
                hybridGrid.innerHTML = createHybridGroupCard(hybridSpecies);
                hybridSection.classList.remove('hidden');
            } else {
                hybridSection.classList.add('hidden');
            }
        }

        // 交配種の園芸品種カードを表示
        function renderHybridSubspecies() {
            const pureSection = document.getElementById('pure-species-section');
            const hybridSection = document.getElementById('hybrid-species-section');
            const pureGrid = document.getElementById('pure-species-grid');

            const hybridSpecies = allSpeciesData.filter(item => item.type === 'hybrid');

            if (hybridSpecies.length > 0) {
                // 交配種を園芸品種カードとして表示
                const subspeciesData = hybridSpecies.map(hybrid => {
                    return {
                        id: hybrid.id,
                        displayName: hybrid.displayName || hybrid.name,
                        tag: hybrid.name || '',
                        count: hybrid.totalPosts || 0,
                        latestImage: hybrid.latestImage,
                        latestPostDate: hybrid.latestPostDate,
                        firstPostDate: hybrid.firstPostDate
                    };
                });

                // 関連度でソート（最終投稿日が新しい＋投稿数が多い順）
                subspeciesData.sort(sortByRelevance);

                const cards = subspeciesData.map(createSubspeciesCard).join('');

                pureGrid.innerHTML = cards;
                pureSection.classList.remove('hidden');
            } else {
                pureGrid.innerHTML = '<p class="text-gray-600 col-span-full text-center py-12">交配種が見つかりませんでした</p>';
                pureSection.classList.remove('hidden');
            }

            hybridSection.classList.add('hidden');
        }

        // 特定の原種の園芸品種カードを表示
        function renderSpeciesSubspecies(speciesId) {
            const pureSection = document.getElementById('pure-species-section');
            const hybridSection = document.getElementById('hybrid-species-section');
            const pureGrid = document.getElementById('pure-species-grid');

            const species = allSpeciesData.find(item => item.id === speciesId);

            if (species && species.subSpecies && species.subSpecies.length > 0) {
                // 品種データをコピーしてソート
                const sortedSubspecies = [...species.subSpecies].sort(sortByRelevance);
                const cards = sortedSubspecies.map(createSubspeciesCard).join('');
                pureGrid.innerHTML = cards;
                pureSection.classList.remove('hidden');
            } else {
                pureGrid.innerHTML = '<p class="text-gray-600 col-span-full text-center py-12">該当する品種が見つかりませんでした</p>';
                pureSection.classList.remove('hidden');
            }

            hybridSection.classList.add('hidden');
        }


        // 統計情報更新
        function updateStats(category) {
            const statsElement = document.getElementById('gallery-stats');
            if (!statsElement) return;

            if (category === 'all') {
                statsElement.textContent = 'Platycerium Growing Records';
            } else if (category === 'hybrid') {
                const hybridSpecies = allSpeciesData.filter(item => item.type === 'hybrid');
                const totalPosts = hybridSpecies.reduce((sum, item) => sum + (item.totalPosts || 0), 0);
                statsElement.textContent = `交配種 / Hybrids - ${hybridSpecies.length}品種 / ${totalPosts}件`;
            } else {
                const species = allSpeciesData.find(item => item.id === category);
                if (species) {
                    const speciesName = speciesNameMap[category] || species.displayName || species.name;
                    const count = species.subSpeciesCount || 0;
                    const posts = species.totalPosts || 0;
                    statsElement.textContent = `${speciesName} - ${count}品種 / ${posts}件`;
                }
            }
        }


        // 園芸品種カードを生成
        function createSubspeciesCard(subspecies) {
            const linkUrl = `/gallery/detail?id=${subspecies.id}`;

            const imageSrc = subspecies.latestImage
                ? `/${subspecies.latestImage}`
                : '/images/placeholder-leaf.svg';

            // 栽培期間を計算
            let cultivationPeriod = '';
            if (subspecies.firstPostDate && subspecies.latestPostDate) {
                const firstDate = subspecies.firstPostDate;
                const lastDate = subspecies.latestPostDate;
                cultivationPeriod = `${firstDate} 〜 ${lastDate}`;
            } else if (subspecies.latestPostDate) {
                cultivationPeriod = subspecies.latestPostDate;
            }

            return `
                <div class="species-card bg-white rounded-lg shadow-md overflow-hidden fade-in"
                     onclick="window.location.href='${linkUrl}'">
                    <img src="${imageSrc}"
                         alt="${subspecies.displayName}"
                         class="species-thumbnail"
                         loading="lazy"
                         onerror="this.src='/images/placeholder-leaf.svg'">
                    <div class="species-card-content p-4">
                        <h3 class="text-lg font-bold text-forest-dark mb-2">
                            ${subspecies.displayName}
                        </h3>
                        <p class="text-sm text-gray-500">
                            ${cultivationPeriod}
                        </p>
                    </div>
                    <div class="species-card-footer px-4 justify-between">
                        <span class="text-sm text-gray-600">📸 ${subspecies.count || 0}件の投稿</span>
                        <svg class="w-4 h-4 text-forest-mid" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                        </svg>
                    </div>
                </div>
            `;
        }

        // 交配種グループカードを生成
        function createHybridGroupCard(allHybrids) {
            if (allHybrids.length === 0) return '';

            // 統計情報を集計
            const totalHybridCount = allHybrids.length;

            // 代表画像を選択（投稿数が最も多い交配種の画像）
            const representativeHybrid = allHybrids.reduce((max, hybrid) =>
                (hybrid.totalPosts || 0) > (max.totalPosts || 0) ? hybrid : max
            , allHybrids[0]);

            const imageSrc = representativeHybrid.latestImage
                ? `/${representativeHybrid.latestImage}`
                : '/images/placeholder-leaf.svg';

            // クリックイベント：ピル型ボタンと同じ動作
            return `
                <div class="species-card pure-species-card bg-white rounded-lg shadow-md overflow-hidden fade-in"
                     onclick="selectCategory('hybrid', event)">
                    <img src="${imageSrc}"
                         alt="交配種 / Hybrids"
                         class="species-thumbnail"
                         loading="lazy"
                         onerror="this.src='/images/placeholder-leaf.svg'">
                    <div class="species-card-content p-4">
                        <h3 class="text-lg font-bold text-forest-dark mb-2">
                            交配種 / Hybrids
                        </h3>
                        <p class="text-sm text-gray-500">
                            ${totalHybridCount}品種
                        </p>
                    </div>
                    <div class="species-card-footer px-4 justify-between">
                        <span class="text-sm text-forest-mid font-semibold">品種を見る</span>
                        <svg class="w-4 h-4 text-forest-mid" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                        </svg>
                    </div>
                </div>
            `;
        }

        // 品種カード生成関数
        function createSpeciesCard(species) {
            // 画像の表示
            let imageSrc = '/images/placeholder-leaf.svg';
            if (species.latestImage) {
                imageSrc = `/${species.latestImage}`;
            } else if (species.subSpecies && species.subSpecies.length > 0 && species.subSpecies[0].latestImage) {
                imageSrc = `/${species.subSpecies[0].latestImage}`;
            }

            // 英語表記を使用
            const displayName = speciesNameMap[species.id] || species.displayName || species.name || species.id;

            // 園芸品種数の表示（原種の場合のみ）
            const subSpeciesCount = species.subSpeciesCount || 0;

            // クリックイベント：ピル型ボタンと同じ動作（カテゴリ選択）
            return `
                <div class="species-card pure-species-card bg-white rounded-lg shadow-md overflow-hidden fade-in"
                     onclick="selectCategory('${species.id}', event)">
                    <img src="${imageSrc}"
                         alt="${displayName}"
                         class="species-thumbnail"
                         loading="lazy"
                         onerror="this.src='/images/placeholder-leaf.svg'">
                    <div class="species-card-content p-4">
                        <h3 class="text-lg font-bold text-forest-dark mb-2">
                            ${displayName}
                        </h3>
                        <p class="text-sm text-gray-500">
                            ${subSpeciesCount}品種
                        </p>
                    </div>
                    <div class="species-card-footer px-4 justify-between">
                        <span class="text-sm text-forest-mid font-semibold">品種を見る</span>
                        <svg class="w-4 h-4 text-forest-mid" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                        </svg>
                    </div>
                </div>
            `;
        }

        // URLハッシュをチェックして該当セクションまでスクロール
        function checkUrlHashAndScroll() {
            const hash = window.location.hash.slice(1); // "#" を除去
            if (!hash) return;

            console.log('URL hash detected:', hash);

            // データが読み込まれていることを確認
            if (!allSpeciesData || allSpeciesData.length === 0) {
                console.warn('Species data not loaded yet');
                return;
            }

            // hashに該当するカテゴリを選択して表示
            if (hash === 'hybrid') {
                // 交配種セクションまでスクロール
                // カテゴリチップをアクティブに
                const chipElement = document.querySelector(`[data-category="hybrid"]`);
                if (chipElement) {
                    // すべてのチップの選択を解除
                    document.querySelectorAll('[data-category]').forEach(btn => {
                        btn.classList.remove('filter-active');
                        btn.style.background = '';
                        btn.style.borderColor = '';
                        btn.style.boxShadow = '';
                    });

                    // 選択されたチップをアクティブに
                    chipElement.classList.add('filter-active');
                    chipElement.style.background = 'linear-gradient(135deg, rgba(16, 185, 129, 0.15), rgba(5, 150, 105, 0.2))';
                    chipElement.style.borderColor = 'rgba(16, 185, 129, 0.5)';
                    chipElement.style.boxShadow = '0 4px 16px rgba(16, 185, 129, 0.2), inset 0 1px 0 rgba(255, 255, 255, 1)';
                }

                // カテゴリを表示
                currentCategory = 'hybrid';
                displayCategory('hybrid');

                // スクロール
                setTimeout(() => {
                    const hybridSection = document.getElementById('hybrid-species-section');
                    if (hybridSection) {
                        hybridSection.scrollIntoView({ behavior: 'smooth', block: 'start' });
                    }
                }, 500);
            } else {
                // 原種の場合、該当するカテゴリを選択
                const speciesExists = allSpeciesData.some(item => item.id === hash);
                if (speciesExists) {
                    // カテゴリチップをアクティブに
                    const chipElement = document.querySelector(`[data-category="${hash}"]`);
                    if (chipElement) {
                        // すべてのチップの選択を解除
                        document.querySelectorAll('[data-category]').forEach(btn => {
                            btn.classList.remove('filter-active');
                            btn.style.background = '';
                            btn.style.borderColor = '';
                            btn.style.boxShadow = '';
                        });

                        // 選択されたチップをアクティブに
                        chipElement.classList.add('filter-active');
                        chipElement.style.background = 'linear-gradient(135deg, rgba(16, 185, 129, 0.15), rgba(5, 150, 105, 0.2))';
                        chipElement.style.borderColor = 'rgba(16, 185, 129, 0.5)';
                        chipElement.style.boxShadow = '0 4px 16px rgba(16, 185, 129, 0.2), inset 0 1px 0 rgba(255, 255, 255, 1)';
                    }

                    // カテゴリを表示
                    currentCategory = hash;
                    displayCategory(hash);

                    // スクロール
                    setTimeout(() => {
                        const pureSection = document.getElementById('pure-species-section');
                        if (pureSection) {
                            pureSection.scrollIntoView({ behavior: 'smooth', block: 'start' });
                        }
                    }, 500);
                }
            }
        }

        // ページロード時に実行
        loadMainSpecies();
    </script>

    <!-- Accordion Toggle -->
    <script is:inline>
        window.toggleAccordion = function(accordionId) {
            const content = document.getElementById(accordionId);
            const icon = document.getElementById(accordionId + '-icon');

            if (content && icon) {
                content.classList.toggle('open');
                icon.classList.toggle('rotate');
            }
        }
    </script>


</BaseLayout>
